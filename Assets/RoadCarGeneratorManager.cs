using UnityEngine;

public class RoadCarGeneratorManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public RoadCarGenerator roadCarGeneratorLeftLane;
    public RoadCarGenerator roadCarGeneratorRightLane;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
